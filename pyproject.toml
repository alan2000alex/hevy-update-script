[project]
name = "hevy-update-tool"
version = "0.1.0"
description = "Correct historical dumbbell weights in Hevy workouts"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.3",
    "typer>=0.25.1",
]

[tool.uv]
package = false
